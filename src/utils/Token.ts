import { Row } from "react-bootstrap";

export function getToken() {
  // const token = document.cookie
  // 	.split("; ")
  // 	.find((row) => row.startsWith("token="))
  // 	?.split("=")[1];
  // console.log(token);

  // if (!token) {
  // 	console.log("No token found");
  // 	return;
  // }
  //const token = document.cookie.split(";").find(r => r.name = "jwt");

  //console.log(token);
  return null;
}
